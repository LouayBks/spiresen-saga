# Spec: Window system (morph-open, breadcrumb, fullview, recursive canvas) (#44)

Format per `DOC/templates/spec_design_template.md` (ADR-006). Formalizes `boxes-plan.md` §9 into BHV-*/CON-*.

**Revision note (2026-09-18, part 1):** the first draft of this spec assumed a group node's window shows flattened, no-id "inline children" (per `application_architecture.md`'s then-current "group-nesting exception") and spent most of its content managing the consequences of that: an auto-scatter layout with no real meaning, a fake-structure risk, and a depth cap to bound it. That underlying model was wrong — corrected in `application_architecture.md` (2026-09-18): a node with children contains a real **nested Map** (its own id doubles as the Map partition key), using the exact same Node/Link/View system as the top level. This draft removes everything that only existed to manage the old model's fake-structure problem, because that problem no longer exists — a nested Map's contents are real Nodes the owner drags and links exactly like the top-level canvas, not an algorithm's guess at where they should sit.

**Revision note (2026-09-18, part 2):** `kind` (including "group") is retired as a stored field (`naming.md`, `content-authoring.md`). Every "group-kind Node" reference below means, precisely: a node with 2+ children, per `content-authoring.md` CON-8's inference order. This revision also adds the grid-vs-freeform layout rule for a nested Map's *default* arrangement, settled in discussion after the first draft.

**Revision note (2026-09-18, part 3): at most one window is open at a time, for now.** Withdraws §9's original window-*stack* model (a new window opens on top of an existing one, the parent auto-fullviews as a backdrop, closing the child reverses it). Instead: exactly one window exists once you've clicked into anything. Navigating deeper (clicking a node inside the open window) or shallower (clicking an earlier breadcrumb segment) **replaces that same window's content** and grows or shrinks the breadcrumb — it never opens a second simultaneous window. The owner can still manually expand the single open window to fullview at any point ("becoming the map" — edge-to-edge, indistinguishable from the outer canvas — while the breadcrumb stays visible so it's clear you're still inside a nested Map, not back at the top). Explicitly scoped "for now" — multi-window stacking isn't rejected forever, just out of scope until there's a real reason to bring it back.

**Revision note (2026-09-18, part 4): window chrome state (windowed vs. fullview, and rect) persists like a desktop OS window, not per-node.** Confirmed in discussion: (1) a nested Map still gets its own ≤3 Views per `alternate-views.md` — what "resolves the parent" so navigation never gets lost is the breadcrumb chain of Node/Map ids, not View state, which each Map resolves independently; (2) fullview persists through navigation, forward or backward, until the owner manually restores it — it is **not** tied to any particular breadcrumb level; (3) windowed-mode size/position is remembered the same way, Windows/macOS-style: maximize once, and the *next* window you open (even for a different node, even in a later session) opens already maximized; resize it while windowed, and that becomes the remembered rect for next time. Stored client-side (`localStorage`, a "the visitor's own browser" preference, not server-side content) — defaults to windowed, at `boxes-plan.md`'s existing default rect, with nothing stored yet.

## Objective

Formalize the window/expansion mechanism — the thing that makes "nodes within nodes" actually buildable and navigable, and the direct answer to whether the app's recursion is covered by a spec anywhere.

## Context

- Ticket: #44.
- Updates once accepted: `boxes-plan.md` §9 only if a real gap is found (none expected — this is a formalization pass, not a redesign).
- Cites, doesn't redecide: `node-link-lifecycle.md` (Node/Link creation, drag, deletion — applies unchanged inside a nested Map), `alternate-views.md` (a nested Map gets the same ≤3-View system as any Map), `box-sizing.md` (tiers — explicitly does not apply to window rects, see CON-1), `content-authoring.md` (article/video *authoring*; this spec owns *reading* an already-authored one).

## Decisions

- **A window's rect is independently resizable — not governed by `box-sizing.md`'s tiers.** A Node's S/M/L tier is its footprint *on the canvas*; the window that opens when you click it is a separate UI surface with its own free-resize rect (min-size floor only). No contradiction with the "no free resize" decision for Nodes — different object, different purpose.
- **A node with children opens a window showing a real nested Map, not a rendering of summary data.** Opening it runs one `Query` on `PK: MAP#{thatNodeId}` (per `application_architecture.md`'s correction) and gets back real Node/Link rows — the same shape a top-level Map's canvas gets. Dragging a Node inside that window *is* `node-link-lifecycle.md`'s existing drag/position behavior; drawing a link between two of them *is* its existing drag-to-link flow. Nothing new is invented here — this spec's only job is confirming those rules apply unchanged one (or more) levels down.
- **Default layout: a grid, not freeform — until the first Link is drawn inside it.** A newly-populated nested Map (no Links yet) arranges its Nodes in a fixed grid, Android-homescreen-widget style: each tier occupies a defined cell span (exact spans not settled — see Open questions), and the grid constrains how a node can size up while ungridded content doesn't exist yet to justify freeform placement. The moment the owner draws the *first* Link inside that Map, it switches to freeform — Nodes go wherever they're dropped from then on, the same as the outer canvas, and the grid is abandoned for that Map permanently (not re-imposed if the Link is later deleted). This directly replaces the withdrawn auto-scatter/depth-cap machinery from the first draft: a grid with no Links doesn't pretend to carry meaning (it's honestly just a grid), so there's nothing dishonest about it, and freeform only activates once the owner has started actually composing real relationships.
- **A nested Map gets the same ≤3-View system as any Map.** `alternate-views.md`'s rules (1–3 Views, per-View positions, View-scoped Links) are written generically ("a Map," never "the top-level Map") — they already apply here without modification. A window for a deeply-nested group can have its own alternate arrangements exactly like the outer canvas can.
- **No depth cap.** The earlier cap existed only to bound the old model's fake-auto-layout problem, which doesn't exist anymore — a nested Map is fetched lazily (one `Query` per level, only when opened), so depth doesn't multiply cost the way embedding everything upfront would have. This matches `boxes-plan.md` §9 as literally written: depth is allowed, and the breadcrumb is the tool that keeps it legible (per the single-window revision below), not a limit on how deep an owner can nest.
- **Everything else in §9 is confirmed as written, not redecided, with one adjustment for the single-window model**: morph-open via FLIP from the clicked element's exact rect (now: morphs the one existing window's content, not a new DOM window, when navigating deeper); fullview as a single state, now reached only manually (the automatic on-stack trigger no longer applies — there's nothing to stack); breadcrumb-not-back-button navigation (now: the breadcrumb drives the single window's content, not which windows are open/closed); article leaves open as a document view, video leaves as a player-frame view; the "opener" convention for click targets inside draggable regions.

## Expected behaviors (`BHV-*`)

| ID | Statement | Testability check | Notes |
|---|---|---|---|
| BHV-1 | WHEN the owner clicks a node visualized as a map (2+ children) or media (attached article/video) on the outer canvas, and no window is currently open, THE UI SHALL open a window that morphs (FLIP) from the clicked element's exact rect to the remembered window state's rect (windowed rect, or fullview if that's the remembered mode). | ✅ — component test: with a stored fullview preference, click a node, assert the window morphs to edge-to-edge; with a stored windowed rect, assert it morphs to that exact rect | Never fades in centered — confirms §9 as written. Trigger updated per `content-authoring.md` CON-8's dynamic inference. Destination rect now comes from remembered state (CON-8), not a fixed default. Clicking such a node *inside* an already-open window is BHV-2, not this row. |
| BHV-2 | WHEN the owner clicks a node inside the already-open window, THE UI SHALL replace that same window's content with the clicked node's Map (or leaf view), extending the breadcrumb by one segment, with the window's current mode (windowed/fullview) and rect UNCHANGED — THE UI SHALL NOT open a second simultaneous window. | ✅ — component test: open a window in fullview, click a node inside it, assert content/breadcrumb update but the window stays fullview | Replaces the withdrawn stacking/auto-fullview-cascade behavior (BHV-2/3 in the first draft). Window chrome state persists across content navigation. |
| BHV-3 | WHEN the owner clicks the maximize control, THE window SHALL toggle fullview, update the remembered window state to match, and — when restoring from fullview — apply the remembered windowed rect (its last known size/position, not a fixed default). | ✅ — component test: resize a window, maximize, restore, assert the restored rect equals the resized (not default) rect | The only way to reach fullview — no automatic trigger remains. |
| BHV-4 | WHEN the owner clicks an earlier breadcrumb segment, THE UI SHALL replace the single window's content with that ancestor level's Map and truncate the breadcrumb to that point, with the window's current mode and rect UNCHANGED. | ✅ — component test: navigate 3 levels deep in fullview, click the 1st-level breadcrumb, assert content/breadcrumb match level 1 and the window is still fullview | Reverses the first draft of this revision, which had breadcrumb navigation exit fullview — corrected: fullview persists through navigation in both directions, per Decisions. |
| BHV-5 | WHEN the owner resizes or moves the window while windowed, THE UI SHALL update the remembered window state to the new rect. | ✅ — component test: resize a window, assert the stored preference reflects the new dimensions | Live update, not just on close — matches Decisions' "resize it while windowed, that becomes the remembered rect for next time." |
| BHV-6 | WHEN the window's content is (re)loaded to show a node-with-children's Map, THE frontend SHALL fetch it via a single `Query` on that Node's own id used as a Map partition key — THE UI SHALL NOT render the new content before that Query resolves. | ✅ — integration test: navigate the window to a node with children, assert exactly one Query fires against `PK: MAP#{nodeId}` before content renders | Replaces the withdrawn "fixed-once layout" rule — there's no precomputed layout to withdraw from; positions are real, per `alternate-views.md`. Applies both on first open (BHV-1) and on navigating deeper/shallower (BHV-2/BHV-4). |
| BHV-7 | WHEN the owner drags a Node inside the open window, THE UI SHALL update its position using the identical mechanism as `node-link-lifecycle.md`/`alternate-views.md` define for the outer canvas. | ✅ — same test as the outer canvas's drag-position test, run against the window's current Map instead | No separate rule — citing, not redefining. |
| BHV-8 | WHEN the owner opens a node visualized as media (at any depth) whose attached content is an Article, THE window SHALL show a document view (hero, meta line, title, scrollable paragraphs) — never a Map. | ✅ — component test: navigate to an Article media node, assert no pan/zoom canvas renders | Reading view; `content-authoring.md` BHV-1 owns the separate editing view. |
| BHV-9 | WHEN the owner opens a node visualized as media (at any depth) whose attached content is a Video, THE window SHALL show a player-frame view with a caption — never a Map. | ✅ — component test: navigate to a Video media node, assert a player frame renders, no canvas | |
| BHV-10 | THE UI SHALL exempt every click target rendered inside a draggable region (window titlebar, canvas Node at any depth) from that region's pointer-capture drag handler, so the target's own click still fires. | ✅ — component test: render a close/expand button inside a draggable titlebar, simulate a click, assert the button's handler fires and no drag started | Structural convention per §9's own build note — a shared "opener" mechanism, not a per-button fix. |
| BHV-11 | WHEN the window's content is (re)loaded to a Map that has zero Links, THE UI SHALL lay its Nodes out in a fixed grid, sized per tier (block span TBD, see Open questions). | ✅ — component test: navigate to a link-less Map, assert nodes render at grid-aligned coordinates | |
| BHV-12 | WHEN the owner draws the first Link inside the currently-shown Map, THE UI SHALL switch that Map's layout to freeform and SHALL NOT revert to grid even if every Link in it is later deleted. | ✅ — integration test: draw a link, assert a layout-mode flag flips to freeform and persists after deleting that link | One-way switch, per Decisions. |
| BHV-13 | WHEN the window's content is (re)loaded to a Map, THE UI SHALL resolve that Map's own active View independently of whatever View was active at the previous breadcrumb level. | ✅ — integration test: set View B active on a nested Map, navigate to a child and back, assert View B is still active (not reset to the parent's or a default) | The breadcrumb (Node/Map id chain) is what "resolves the parent," per Decisions — View state doesn't thread through it. |

## Constraints (`CON-*`)

| ID | Statement | Notes |
|---|---|---|
| CON-1 | A window's rect (position/size) MUST be independently resizable and MUST NOT be constrained to `box-sizing.md`'s S/M/L tiers. | Different object (window vs. Node footprint), different rule. |
| CON-2 | Fullview MUST be a single visual/interaction state, reached only via the manual maximize control — there MUST NOT be a distinct "automatically fullviewed" appearance. | Simplified now that stacking (the old automatic trigger) is withdrawn — see CON-7. |
| CON-8 | The window's mode (windowed/fullview) and rect MUST be a single persistent client-side preference — MUST NOT reset on content navigation (forward or backward), MUST NOT reset when a new window is opened later, and MUST NOT be tied to any specific Node/Map. | Windows/macOS-style window memory, per Decisions part 4. Governs BHV-1's destination rect and BHV-2/BHV-4's "unchanged" requirement. |
| CON-9 | A Map's active View MUST be resolved independently at each breadcrumb level — navigating away and back MUST NOT reset it to a default or inherit the parent level's active View. | Answers "how do we not get lost": the breadcrumb (Node/Map id chain) resolves ancestry; View state is separate and per-Map. |
| CON-3 | A node-with-children's contents MUST be real Node/Link/View rows, addressable and authorizable exactly like a top-level Map's — MUST NOT be flattened into non-addressable summary data. | Reverses the withdrawn CON-4 from the first draft; restates `application_architecture.md`'s correction in this spec's own terms. |
| CON-4 | A node's own id MUST double as its nested Map's partition key (`PK: MAP#{nodeId}`) once it has children — MUST NOT require a separate stored reference to find it. | |
| CON-5 | Every draggable region's pointer-capture handler MUST exempt any descendant explicitly tagged as an opener, rather than relying on a per-button ad hoc check. | |
| CON-6 | A nested Map's layout mode (grid or freeform) MUST be a one-way switch — once freeform, it MUST NOT revert to grid even if the Map's Link count later drops back to zero. | |
| CON-7 | At most one window MUST be open at a time — navigating into a deeper node or an earlier breadcrumb segment MUST replace the currently-open window's content, MUST NOT open an additional simultaneous window. | The core rule of this revision. Stated as "for now" in Decisions — a deliberate scope limit, not a permanent rejection of multi-window stacking. |

## Open questions

- **#40's scope needs widening.** It was written assuming View-switching UI exists only on the outer canvas; per this spec's Decisions, a nested Map can have its own Views too (resolved independently per CON-9), so the switching UI needs to exist inside the window as well. Not resolved here — flagging for whoever picks up #40.
- **Grid block spans, per tier, not yet defined.** Android-widget-style (S/M/L → defined cell spans), but the exact numbers, and what happens when a node's tier is changed while it's still gridded (does the grid reflow, or is tier capped while gridded?), need their own design pass — "to be studied," in your words, not decided here.

## Out of scope

No implementation (that's #38). Exact FLIP animation timing/easing, breadcrumb truncation behavior at very deep paths, and document/player-view visual styling are implementation details, not modeling decisions.
